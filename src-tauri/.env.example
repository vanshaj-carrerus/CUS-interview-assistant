# Public API for auth + interview AI (production builds)
VITE_API_URL=https://www.custech.co

# Groq Whisper STT — prefer: npm run env:sync (from GitHub secret VITE_GROQ_API_KEY / GROQ_API_KEY)
# VITE_GROQ_API_KEY=

# Audio source: display = share screen/tab with system audio (default, matches legacy loopback)
# microphone = mic only
# VITE_STT_AUDIO_SOURCE=display
