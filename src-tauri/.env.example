# Copy to src-tauri/.env for local dev and `tauri build`.
# For GitHub releases, add the same names as repository secrets (see README).

VITE_GEMINI_API_KEY=
VITE_MISTRAL_API_KEY=
VITE_GROQ_API_KEY=
VITE_OPENROUTER_API_KEY=
